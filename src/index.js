import React from 'react';
import ReactDOM from 'react-dom';
import ParentComponent from './components/Form'

ReactDOM.render(
  <div>
    <ParentComponent />
  </div>,
  document.getElementById('root')
);